# AION-CX 360° Analytics Dashboard

## 📊 Overview
Real-time dashboard for visualizing lead intelligence from the AION-CX system. Built with pure HTML/CSS/JS (no external libraries).

## 🚀 How to Run
1.  **Start Backend**: Ensure `node server.js` is running in `/backend`.
2.  **Open Dashboard**: Open `index.html` in your browser.
3.  **Verify**: Check that the counters, table, and chart populate with data.

## ⚙️ Configuration
-   **API URL**: Default is `http://localhost:3000/dashboard/overview`.
-   If using **ngrok**, update the `API_URL` constant in the `<script>` section of `index.html`.
